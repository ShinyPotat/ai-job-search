{
  "name": "infojobs-cli",
  "version": "1.0.0",
  "description": "CLI for searching jobs on InfoJobs (Spain) via web scraping with Defuddle. Personal use only.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "infojobs-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test --timeout 30000"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
